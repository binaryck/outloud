type CreatePostPageProps = {
  isInscribing: boolean;
};

export const CreatePostPage = ({ isInscribing }: CreatePostPageProps) => {
  return (
    <div>
      <h1>Create Post</h1>
    </div>
  );
};
